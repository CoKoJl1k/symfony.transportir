<h1>Your title  is {{ title }}</h1>

<h1>Your blog number is {{ number }}</h1>
<p>{% set route_name = app.request.attributes.get('_route') %}</p>
<p>{% set route_parameters = app.request.attributes.get('_route_params') %}</p>
<p>{% set all_attributes = app.request.attributes.all %}</p>

{# use this to get all the available attributes (not only routing ones) #}

